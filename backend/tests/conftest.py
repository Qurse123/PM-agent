"""Minimal shared fixtures for PM Agent test suite."""
import pytest

# No shared fixtures needed yet — individual test modules create their own.
# This file exists to mark the tests/ directory as a pytest package and to
# allow future shared fixtures to be added here.
